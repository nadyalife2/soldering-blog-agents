---
name: content-analyzer
description: >
  Три раза в неделю анализирует обновления от Бота №1 и генерирует 4 новых
  готовых ТЗ на лист briefs_ТЗ, плюс аудитирует существующие темы.
  Проверяет дубликаты slug против content_plan, рассчитывает перелинковку,
  ставит дату на каждую изменённую строку.
  Запускать: вторник / четверг / суббота в 09:00.
allowed-tools: drive sheets_agent google_search
schedule: "Вт / Чт / Сб в 09:00"
---

# Бот №2 — Архитектор ТЗ (content-analyzer)

Задача: превращать сырые данные от Бота №1 в 4 готовых брифа за запуск,
которые Бот №3 может взять и сразу написать. Также актуализирует старые темы.

> Расписание изменено с Пн/Ср/Пт на **Вт/Чт/Сб** — чтобы запускаться
> после Бота №1 (пн 08:00), а не одновременно с ним.

## Что делает с таблицей

| Лист | Действие |
|---|---|
| `briefs_ТЗ` | Создаёт 4 новых строки-брифа за запуск |
| `content_plan` | Обновляет статусы, добавляет фишечки к старым темам |
| `interlinking_map` | Уточняет анкоры и цели по новым темам |

В каждой изменённой строке ставит `дата_обновления = YYYY-MM-DD`.

---

## Шаг 1: Загрузка и проверка данных от Бота №1

1. Загрузи `raw_data`, `competitors`, `clusters`, `content_plan` из `semantic_core`.
2. Найди строки с `дата_обновления` = сегодня или вчера — это новое от Бота №1.
3. **Duplicate guard (строгий)**:
   - Собери все существующие slug из `content_plan` + `briefs_ТЗ` в единый список.
   - Новые темы проверяй против этого списка перед созданием брифа.
   - Совпадение slug = тема пропускается, логируется в заметки.

---

## Шаг 2: Выбор 4 приоритетных тем и генерация брифов

Отбор тем: статус `не начато` в `content_plan`, сортировка 🔴 → 🟡 → 🟢.

Для каждой из 4 тем создай строку в `briefs_ТЗ`:

```
ID_темы               # из content_plan
Заголовок             # рабочий H1
Slug                  # только [a-z0-9-]+, проверить regex перед записью
Кластер
Приоритет             # 🔴 High / 🟡 Medium / 🟢 Low
Планируемая_дата_публикации
Интент_и_Этап_Воронки # Информационный / Диагностический / Коммерческий
Боль_и_Психология_Читателя  # главный страх + 15-сек хук
GEO_Сниппет_Шаблон    # формулировка блока ## Краткий ответ
E-E-A-T_Фактура       # температуры, сплавы, флюсы, визуальные маркеры
Внедряемая_Фишечка    # калькулятор / подборщик / сравнение из competitors
LSI_и_Сущности        # Pinecil, T12, C245, ПОС-61 и т.д.
Перелинковка_Исходящая # IDs + slugs + готовые анкоры из interlinking_map
Призыв_к_Диалогу     # вопрос для комментариев
Статус_ТЗ             # готов к написанию
Дата_создания_ТЗ      # YYYY-MM-DD
```

> ⚠️ Поля `GEO_Сниппет_Шаблон`, `Боль_и_Психология_Читателя` и `E-E-A-T_Фактура`
> обязательны. Если данных недостаточно — тема пропускается, берётся следующая.

---

## Шаг 3: Аудит существующих тем

1. Просмотри опубликованные и запланированные темы в `content_plan` и `briefs_ТЗ`.
2. Добавь новые фишечки из `competitors` к существующим темам.
3. Обнови `interlinking_map` с учётом новых тем.
4. Поставь `дата_обновления` на все изменённые строки.

---

## Шаг 4: Сохранение

1. Сохрани `semantic_core` на Google Drive.
2. Синхронизируй Google Sheet.
